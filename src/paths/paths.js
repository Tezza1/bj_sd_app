import React from 'react';

const Paths = () => {
    return(
        <div style={{marginTop: "2em", paddingTop: "2em"}}>
            <h1>Paths</h1>    
        </div>
    );
};

export default Paths;