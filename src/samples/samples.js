import React from 'react';

const Samples = () => {
    return(
        <div style={{marginTop: "2em", paddingTop: "2em"}}>
            <h1>Samples</h1>    
        </div>
    );
};

export default Samples;